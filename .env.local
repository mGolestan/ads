# Port to start application on
PORT=8080

# The origins that are allowed to access this API (lock this down for production)
CORS_ORIGIN=*

# Set the current logging level, `debug` is useful for development environments
# Possible values are: error, warn, info, verbose, debug
LOG_LEVEL=debug
